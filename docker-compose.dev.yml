version: "3.6"
services:
  server:
    build:
      context: .
      dockerfile: dev.dockerfile
    env_file: .env
    volumes:
      - .:/usr/src/app/
      - /usr/src/app/node_modules
    restarts: "always"
    ports:
      - "3001:3002" 